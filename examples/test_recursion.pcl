program kkk;
	var x : integer;

	function fib(n : integer) : integer;
		begin
			if n < 3 then
				result := 1
			else
				result := fib(n - 1) + fib(n - 2) + x
		end;
	begin
		x := 1;
		writeInteger(fib(10));
		writeChar('\n');
	end.
