program expr;
	var x : integer;
	var y : integer;
	procedure kao();
		begin
			var x : real;
			writeString("sdf");
			writeReal(x / y);
		end;
	procedure kai();
		var y : real;
		function TruncReal(x : real) : integer;
			begin
				 result := trunc(x);
			end;
		begin
			y := 
			writeInteger(x);
			TruncReal(y);
		end;
begin
	writeInteger(kai(x);
end.
