program expr;
	var x : integer;
	var y : integer;
	procedure kao();
		var x : real;
		begin
			writeString("sdf");
			writeReal(x / y);
		end;
	procedure kai();
		var y : real;
		function TruncReal(x : real) : integer;
			begin
				 result := trunc(x);
			end;
		begin
			y := 0.1;
			writeInteger(x);
			writeInteger(TruncReal(y));
		end;
begin
	kai();
end.
